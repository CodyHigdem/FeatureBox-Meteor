if (Meteor.isClient) {
	/*
Template.editFeature.helpers({
  beforeRemove: function () {
    return function (collection, id) {
      var doc = collection.findOne(id);
      if (confirm('Really delete Feature: "' + doc.brand + " " + doc.model + '"?')) {
      this.remove();
      Router.go('FeaturesList');
    }
  };
 }
});
*/
}